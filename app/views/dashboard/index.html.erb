</h1>

<p>
  <%=@user['first_name']%>'s favourite movies:

  <% if Rails.env.production? %>
  <ul>
    <% @likes.each do |like| %>
      <li>
        <%= like['name'] %> (<%= like['category'] %>)
      </li>
    <% end %>
  </ul>
  <% end %>
</p>
