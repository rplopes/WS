</h1>

<p>
  <%=@user['first_name']%>'s likes:

  <% if Rails.env.production? %>
  <ul>
    <% @likes.each do |like| %>
      <li>
        <%= like['name'] %> (<%= like['category'] %>)
      </li>
    <% end %>
  </ul>
  <% end %>
</p>
